Use kubectl command to check network connectivity to ensure network issue for deployment ‘web’ and namespace "robot-shop" that use host "k8s_nodes", and display the deployment details in JSON format.
