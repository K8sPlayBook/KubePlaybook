Use kubectl command to  restart the pods to solve the process crash process for deployment ‘web’ and namespace "robot-shop" that use host "k8s_nodes", and display the deployment details in JSON format.

